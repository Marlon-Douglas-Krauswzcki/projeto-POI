spring:
  datasource:
    username: marlon
    password: ti.mudar
    url: jdbc:mysql://localhost:3306/poi?useTimezone=true&serverTimezone=UTC
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:      ddl-auto: update
    properties.hibernate.dialect: org.hibernate.dialect.MySQL5Dialect